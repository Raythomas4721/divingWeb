export interface sitedetail {
  id: number; // 添加 id 屬性
  venueName: string;
  numberOfPeople: number;
  venueAddress: string;
  detail: string;
  photo: ArrayBuffer;
  evaluate: string;
  collect: number;
}
