import { LiteralToBreakPipe } from './literal-to-break.pipe';

describe('LiteralToBreakPipe', () => {
  it('create an instance', () => {
    const pipe = new LiteralToBreakPipe();
    expect(pipe).toBeTruthy();
  });
});
